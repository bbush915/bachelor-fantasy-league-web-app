export type LeagueContext = {
  seasonId: string;
  weekNumber: number;
  currentSeasonWeekId: string;
  previousSeasonWeekId?: string;
  leagueId: string;
  leagueName: string;
  leagueMemberId: string;
};
